<?php

use Myth\Controllers\ThemedController;

/**
 * Cows Controller
 *
 * Auto-generated by Sprint on 2020-11-21 10:33am
 */
class Cows extends ThemedController {

    /**
     * The type of caching to use. The default values are
     * set globally in the environment's start file, but
     * these will override if they are set.
     */
    protected $cache_type      = null;
    protected $backup_cache    = null;

    // If set, this language file will automatically be loaded.
    protected $language_file   = null;

    // If set, this model file will automatically be loaded.
    protected $model_file      = 'cow_model';

    
    /**
     * Allows per-controller override of theme.
     * @var null
     */
    protected $theme = null;

    /**
     * Per-controller override of the current layout file.
     * @var null
     */
    protected $layout = null;

    /**
     * The UIKit to make available to the template views.
     * @var string
     */
    protected $uikit = '';

    /**
     * The number of rows to show when paginating results.
     * @var int
     */
    protected $limit = 25;

    //--------------------------------------------------------------------
    public function __construct()
    {
        parent::__construct();
        $this->load->library('session');
    }
    //--------------------------------------------------------------------

    /**
     * The default method called. Typically displays an overview of this
     * controller's domain.
     *
     * @return mixed
     */
    public function index()
    {
        $this->load->library('table');

        $offset = $this->uri->segment( $this->uri->total_segments() );

        $rows = $this->cow_model->limit($this->limit, $offset)
                     ->as_array()
                     ->find_all();
        $this->setVar('rows', $rows);

		$this->render();
    }

    //--------------------------------------------------------------------

    /**
     * Create a single item.
     *
     * @return mixed
     */
    public function create()
    {
        $this->load->helper('form');
        $this->load->helper('inflector');

        if ($this->input->method() == 'post')
        {
            $post_data = $this->input->post();

            if ($this->cow_model->insert($post_data) )
            {
                $this->setMessage('Successfully created item.', 'success');
                redirect( site_url('cows') );
            }

            $this->setMessage('Error creating item. '. $this->cow_model->error(), 'danger');
        }

		$this->render();
    }

    //--------------------------------------------------------------------

    /**
     * Displays a single item.
     *
     * @param  int $id  The primary_key of the object.
     * @return mixed
     */
    public function show($id)
    {
        $item = $this->cow_model->find($id);

        if (! $item)
        {
            $this->setMessage('Unable to find that item.', 'warning');
            redirect( site_url('cows') );
        }

        $this->setVar('item', $item);

		$this->render();
    }

    //--------------------------------------------------------------------

    /**
     * Updates a single item.
     *
     * @param  int $id  The primary_key of the object.
     * @return mixed
     */
    public function update($id)
    {
        $this->load->helper('form');
        $this->load->helper('inflector');

        if ($this->input->method() == 'post')
        {
            $post_data = $this->input->post();

            if ($this->cow_model->update($id, $post_data))
            {
                $this->setMessage('Successfully updated item.', 'success');
                redirect( site_url('cows') );
            }

            $this->setMessage('Error updating item. '. $this->cow_model->error(), 'danger');
        }

        $item = $this->cow_model->find($id);
        $this->setVar('item', $item);

		$this->render();
    }

    //--------------------------------------------------------------------

    /**
     * Deletes a single item
     *
     * @param  int $id  The primary_key of the object.
     * @return mixed
     */
    public function delete($id)
    {
        if ($this->cow_model->delete($id))
        {
            $this->setMessage('Successfully deleted item.', 'success');
            redirect( site_url('cows') );
        }

        $this->setMessage('Error deleting item. '. $this->cow_model->error(), 'danger');
        redirect( site_url( 'cows' ) );
		
    }

    //--------------------------------------------------------------------

    public function testHmvc()
    {
        $this->load->model("airports/Airport_model");
        $res = $this->Airport_model->find_all();
        var_dump($res);

//        $this->load->controller("airports/airports");
        Myth\modules::load_file( 'Airports', APPPATH.'Modules/airports/controllers/');  // get the include() file

        $this->airports = new Airports(true);   //skip inherited contructors
        var_dump($this->airports->airportTestHMVC());
    }
}
